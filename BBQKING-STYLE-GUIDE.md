# BBQKing 風格優化實現文檔

## 專案概述

本文檔說明如何參考 BBQKing 網站的設計風格，對夯肉 HANGROU 網站進行全面優化。

---

## 📋 優化項目清單

### ✅ 1. 全屏輪播 Banner (Hero Slider)

**實現位置**: `index.html` 214-252 行

**設計特點**:
- 自動輪播，每 5 秒切換一次
- 平滑淡入淡出過渡效果
- 導航圓點可點擊切換
- 鼠標懸停時暫停輪播
- 響應式全屏設計

**技術實現**:
```html
<div class="hero-slider">
    <div class="hero-slide active">
        <div class="slide-bg" style="background-image: url(...)"></div>
        <div class="slide-overlay"></div>
        <div class="hero-content">...</div>
    </div>
</div>
<div class="slider-dots">...</div>
```

**CSS 重點**:
- `.hero-slide` 使用 `opacity` 實現淡入淡出
- `.slide-bg` 固定背景圖片 `background-attachment: fixed`
- `.dot.active` 放大效果 `transform: scale(1.3)`

**JavaScript 功能**:
```javascript
// 核心功能
- showSlide(index): 切換到指定幻燈片
- nextSlide(): 自動播放下一張
- startSlideShow(): 啟動輪播（5秒間隔）
- stopSlideShow(): 暫停輪播
- 鼠標懸停控制
```

**可自定義**:
- 修改輪播間隔時間: `setInterval(nextSlide, 5000)` 中的 `5000` 改為其他毫秒數
- 添加更多幻燈片: 複製 `.hero-slide` 元素
- 更換圖片: 修改 `style="background-image: url(...)"`

---

### ✅ 2. 三大服務卡片 (About Cards)

**實現位置**: `index.html` 254-294 行

**設計特點**:
- 大圖片 + 圖文結合布局
- 懸停時圖片放大效果
- "more →" 鏈接按鈕帶下劃線動畫
- 卡片整體上移陰影效果

**BBQKing 風格對應**:
- 到府代客烤肉 → 正宗日式炭火
- 優選生鮮宅配 → 新鮮精選食材
- 專人服務 → 親切現場服務

**HTML 結構**:
```html
<div class="about-card">
    <div class="about-image">
        <img src="..." alt="...">
    </div>
    <div class="about-content">
        <h3>標題</h3>
        <p>描述文字</p>
        <a href="#..." class="more-link">more →</a>
    </div>
</div>
```

**CSS 動畫效果**:
- `transform: scale(1.1)` - 圖片懸停放大
- `translateY(-8px)` - 卡片上移
- `::after` 偽元素 - more 按鈕下劃線動畫

---

### ✅ 3. 活動照片展示區 (Gallery Grid)

**實現位置**: `index.html` 728-768 行

**設計特點**:
- 網格布局 (Responsive Grid)
- 正方形比例圖片 (`aspect-ratio: 1`)
- 懸停放大效果
- 陰影和圓角設計

**BBQKing 對應**:
- 標題: "活動照 | 聚餐 · 燒烤 · 派對"
- 網格展示多張活動照片

**CSS 實現**:
```css
.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
}

.gallery-item {
    aspect-ratio: 1;  /* 正方形 */
    overflow: hidden;
    border-radius: 12px;
}
```

**自定義建議**:
- 更換為真實活動照片
- 調整網格列數: 修改 `minmax(250px, 1fr)` 中的 `250px`
- 添加燈箱效果: 可整合 lightbox.js

---

### ✅ 4. 優化菜單卡片樣式 (Menu Cards)

**實現位置**: `css/style.css` 534-564 行

**新增效果**:
- **頂部漸變條**: 懸停時從左到右展開
- **邊框高亮**: 懸停時變為主色調
- **陰影增強**: 更強的立體感

**CSS 實現**:
```css
.menu-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg,
        var(--primary-color),
        var(--secondary-color));
    transform: scaleX(0);
    transition: transform 0.3s ease;
}

.menu-item:hover::before {
    transform: scaleX(1);
}
```

**視覺改進**:
- 懸停時圖片放大 `scale(1.1)`
- 頂部漸變色條 `linear-gradient`
- 邊框顏色變化 `border-color: var(--primary-color)`

---

### ✅ 5. 優化聯絡信息區域 (Contact Cards)

**實現位置**: `css/style.css` 821-857 行

**BBQKing 風格對應**:
- 客服專線 → 電話訂餐
- LINE → (可添加)
- 門市地址 → 地址
- 門市服務時間 → 營業時間

**新增效果**:
- **左側彩色條**: 懸停時從上到下展開
- **邊框高亮**: 主色調邊框
- **增強位移**: 更明顯的右移效果

**CSS 實現**:
```css
.contact-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: var(--primary-color);
    transform: scaleY(0);
    transition: transform 0.3s ease;
}

.contact-item:hover::before {
    transform: scaleY(1);
}

.contact-item:hover {
    transform: translateX(8px);
    border-color: var(--primary-color);
}
```

---

## 🎨 BBQKing 設計風格總結

### 視覺特點
1. **大圖片展示**: 首屏使用全屏輪播大圖
2. **卡片式布局**: 內容區塊採用卡片設計
3. **暖色調配色**: 橙紅色系 (#FF6B35)
4. **清晰導航**: 圓點導航、more 按鈕
5. **懸停動畫**: 放大、陰影、位移效果

### 交互特點
1. **平滑過渡**: 所有動畫使用 `transition`
2. **漸進式動畫**: 淡入淡出、縮放、位移
3. **視覺反饋**: 懸停時顏色、陰影變化
4. **用戶控制**: 輪播可暫停、可手動切換

---

## 🛠️ 實現技術

### HTML5 語義化標籤
- `<section>` - 區塊容器
- `<nav>` - 導航欄
- `<footer>` - 頁腳

### CSS3 現代特性
- CSS 變數 (`:root`)
- Flexbox 和 Grid 布局
- 偽元素 (`::before`, `::after`)
- 變換 (`transform`)
- 過渡 (`transition`)

### JavaScript ES6+
- 箭頭函數
- `forEach` 迭代
- `setInterval` 定時器
- 事件監聽器

---

## 📱 響應式設計

### 斷點設置
```css
@media (max-width: 968px)  /* 平板 */
@media (max-width: 768px)  /* 手機 */
@media (max-width: 480px)  /* 小手機 */
```

### 自適應佈局
- 輪播: 全屏響應
- 卡片: `grid-template-columns: repeat(auto-fit, minmax(...))`
- 圖片: `object-fit: cover`

---

## 🎯 下一步優化建議

### 必要更新
1. **替換圖片**
   - 使用真實的燒烤菜品照片
   - 添加實際活動照片
   - 更新背景圖片

2. **更新聯絡信息**
   - 添加真實 LINE ID
   - 更新電話號碼
   - 添加社群媒體連結

### 進階優化
1. **整合 LINE 登入/分享**
   ```html
   <a href="https://line.me/R/ti/@yourlineid">
       <img src="line-button.png" alt="Add LINE">
   </a>
   ```

2. **添加燈箱效果**
   - 整合 Lightbox.js
   - 點擊活動照片放大查看

3. **表單功能**
   - 整合後端 API
   - 添加表單驗證
   - 發送郵件通知

4. **SEO 優化**
   - 添加結構化數據（已有）
   - 優化圖片 alt 文字
   - 添加 sitemap.xml

5. **性能優化**
   - 圖片懶加載（已實現 `loading="lazy"`）
   - 壓縮圖片文件
   - 使用 CDN 加速

---

## 🔧 自定義指南

### 更改主色調
編輯 `css/style.css` 的 `:root`:
```css
:root {
    --primary-color: #FF6B35;    /* 主色調 */
    --secondary-color: #FF8C42;  /* 輔助色 */
    --accent-color: #2D3436;     /* 點綴色 */
}
```

### 修改輪播速度
編輯 `js/main.js`:
```javascript
function startSlideShow() {
    slideInterval = setInterval(nextSlide, 5000); // 改為其他毫秒數
}
```

### 調整動畫效果
```css
/* 加快動畫速度 */
transition: all 0.2s ease;

/* 減少上移距離 */
transform: translateY(-5px);

/* 調整放大倍數 */
transform: scale(1.05);
```

---

## 📚 參考資源

### BBQKing 網站分析
- **URL**: https://www.bbqkingtw.com/
- **主要特色**: 烤肉食材、代客烤肉服務
- **設計風格**: 大圖片、卡片式、暖色調

### 技術文檔
- [MDN Web Docs](https://developer.mozilla.org/)
- [CSS Tricks](https://css-tricks.com/)
- [W3Schools](https://www.w3schools.com/)

### 工具推薦
- **圖片壓縮**: TinyPNG, Squoosh
- **圖片來源**: Unsplash, Pexels
- **SEO 工具**: Google Search Console
- **性能測試**: PageSpeed Insights, Lighthouse

---

## 📝 總結

本次優化成功將 BBQKing 的設計風格應用到夯肉 HANGROU 網站，主要完成：

1. ✅ 全屏輪播 Banner - 提升視覺衝擊力
2. ✅ 三大服務卡片 - 圖文結合，清晰展示服務
3. ✅ 活動照片展示 - 網格布局，展示實際案例
4. ✅ 優化菜單卡片 - 增強交互效果
5. ✅ 優化聯絡區域 - 提升用戶體驗

所有改進都保持了響應式設計、暗黑模式支持和良好的 SEO 結構。

---

**文檔更新日期**: 2026-01-20
**版本**: 1.0
**作者**: Claude Code Assistant
